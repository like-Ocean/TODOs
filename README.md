# TODO API + WebSocket + Background Tasks


## Возможности

- **REST API** - CRUD операции для управления задачами
- **WebSocket** - Real-time уведомления о создании/обновлении/удалении задач
- **Background Tasks** - Автоматическая загрузка задач из внешнего API (DummyJSON)
- **Async PostgreSQL** - Асинхронная работа с базой данных
- **HTTP Client** - Интеграция с внешними API через httpx
- **WebSocket Tester** - HTML клиент для тестирования

## 🚀 Быстрый старт

### Вариант 1: С Docker (рекомендуется)

```
# Клонируйте репозиторий
git clone <your-repo-url>
cd TODO

# Создайте .env файл
cp .env.example .env

# Собрать образы
docker-compose build

# Запустите через Docker Compose
docker-compose up -d

# Проверьте статус
docker-compose ps

# Остановить контейнеры
docker-compose down

# Перезапустить
docker-compose restart

# Пересобрать и перезапустить
docker-compose up -d --build

# Смотрите логи
docker-compose logs -f
```


Приложение доступно на: Swagger: http://localhost:8000/docs

### Вариант 2: Локальный запуск

```
# Создайте виртуальное окружение
python -m venv .venv

# Активируйте виртуальное окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Настройте PostgreSQL (убедитесь, что сервер запущен):

- Создайте базу данных TODO
- Создайте .env файл
- Отредактируйте .env с вашими настройками БД




